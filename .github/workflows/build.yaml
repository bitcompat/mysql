name: build

on:
  push:
    branches:
      - main

jobs:
  build_mysql_8_0:
    uses: bitcompat/base/.github/workflows/build.yaml@main
    with:
      name: main
      cache-scope: mysql-80
      build-args: |
        SERVER_VERSION=8.0.32
      image_tag: |
        latest
        8.1.0-bullseye-r1 # renovate: datasource=github-tags depName=mysql/mysql-server
        8.1.0-bullseye # renovate: datasource=github-tags depName=mysql/mysql-server
        8.1.0 # renovate: datasource=github-tags depName=mysql/mysql-server
        8.0
        8
    secrets: inherit

  build_mysql_5_7:
    uses: bitcompat/base/.github/workflows/build.yaml@main
    with:
      name: main
      cache-scope: mysql-57
      build-args: |
        SERVER_VERSION=5.7.41
      image_tag: | 
        5.7.41-bullseye-r1
        5.7.41-bullseye
        5.7.41
        5.7
        5
    secrets: inherit
